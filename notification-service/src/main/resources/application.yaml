spring:
  profiles:
    default: local
  application:
    name: notification-service

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

