spring:
  datasource:
    url: jdbc:postgresql://auth-db:5432/meditrack_authserver
    username: meditrack_user
    password: meditrack123

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka

app:
  issuer: http://auth-server:9000

management:
  tracing:
    sampling:
      probability: 1.0
    propagation:
      type: b3

  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans